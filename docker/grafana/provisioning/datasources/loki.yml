# ============================================================
#  Grafana 数据源 — 自动配置 Loki（内置 Explorer 可直接查日志）
# ============================================================

apiVersion: 1

datasources:
  - name: Loki
    uid: loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
    jsonData:
      maxLines: 5000
